# خطة بناء صفحة الموارد البشرية في بوابة الجمعيات

| البند | التفصيل |
|---|---|
| التاريخ | 17 أغسطس 2026 |
| النطاق | `/portal/[name]/hr` — إدارة حسابات موظفي الجمعية + صلاحيات الوصول + نظام التحضير الإلكتروني بتحديد الموقع |
| الحالة | **خطة للمراجعة — لم يُكتب أي كود** |

---

## 1. الوضع الحالي (مُتحقَّق منه من الكود)

قبل أي تصميم، هذه حقائق فحصتها في المستودع لا افتراضات:

| الحقيقة | الدليل |
|---|---|
| صفحة الموارد البشرية **غير موجودة** | `CharitySidebar.tsx:173` تعرضها كـ`comingSoon: true` و`href: "#"` |
| **لا يوجد أي كود تحديد موقع أو تحضير** في المشروع كله | بحث عن `geolocation`/`latitude`/`attendance` → صفر نتائج |
| حقل `CharityUser.permissions` **موجود في المخطط لكنه غير مُستخدَم إطلاقًا** | لا يوجد أي `hasPermission` أو فحص صلاحية لحسابات الجمعيات في `src/app/portal/**` |
| **لا توجد طبقة صلاحيات لجانب الجمعية أصلًا** | كل عناصر القائمة الجانبية تظهر لكل حسابات الجمعيات بلا استثناء |
| `CharityUserTitle.SYSTEM_ADMIN` **موجود مسبقًا** | `schema.prisma:86` — هذا هو "مدير النظام في الجمعية" المطلوب |
| حسابات الجمعيات تُنشأ اليوم من **موظفي زاد فقط** | `actions/charityAccounts.ts` يشترط صلاحية `manage_charity_accounts` |
| `getSession()` تُحدِّث `permissions` و`title` من قاعدة البيانات في كل طلب | `lib/auth.ts:42-53` — أي تعديل صلاحية يسري فورًا بلا إعادة تسجيل دخول ✅ |
| توجد أدوات تاريخ بتوقيت الرياض جاهزة للاستخدام | `lib/businessDays.ts` (`toCivilDate`, `isBusinessDay`) |
| `AuditActorType` يتضمن `CHARITY_USER` مسبقًا | `schema.prisma` — سجل التدقيق جاهز لتسجيل عمليات الجمعيات |

**الخلاصة:** المطلب الثاني (صلاحيات الوصول) ليس "إضافة صلاحيات" بل **بناء طبقة الصلاحيات لجانب الجمعية من الصفر**.

---

## 2. ⚠️ تحذير جوهري يجب قراءته قبل الموافقة

### تحديد الموقع في المتصفح قابل للتزوير بسهولة تامة

نظام التحضير المطلوب يعتمد على `navigator.geolocation` في المتصفح. **هذا غير قابل للتأمين ضد التزوير، ولا يمكن لأي كود أكتبه أن يمنعه.**

الطريقة عمليًا:
1. يفتح الموظف أدوات المطور في Chrome (زر واحد)
2. تبويب **Sensors → Location → Custom location**
3. يكتب إحداثيات مقر العمل
4. يضغط تحضير من بيته

لا تتطلب أي خبرة تقنية، ولا يمكن كشفها من الخادم بشكل موثوق. تطبيقات الجوال الأصلية تحل هذا عبر **device attestation** (Play Integrity / App Attest) وهي غير متاحة لتطبيق ويب.

### ما الذي تعنيه الخطة إذًا؟

النظام المقترح هو **رادع + سجل موثّق**، وليس نظامًا مانعًا للتزوير:
- يمنع التحضير العَرَضي من خارج الموقع (الغالبية العظمى من الحالات)
- يُنشئ سجلًا قابلًا للمراجعة (إحداثيات، دقة القراءة، IP، الجهاز) يُحاسَب عليه الموظف
- يرصد الأنماط المشبوهة تلقائيًا (سفر مستحيل بين تحضيرين)

### إن كان المطلوب منع التزوير فعليًا

هذه بدائل أقوى بكثير، أوصي بمناقشتها قبل البدء:

| البديل | القوة | الكلفة |
|---|---|---|
| **قائمة IP مسموحة لشبكة المقر** | 🟢 عالية — تزوير IP الشبكة أصعب بمراحل من GPS | منخفضة جدًا، ويمكن إضافتها لهذه الخطة كطبقة ثانية |
| رمز QR متغيّر معروض على شاشة في المقر | 🟢 عالية | متوسطة |
| جهاز بصمة/بطاقة في المقر | 🟢 الأعلى | عالية (عتاد) |
| تطبيق جوال أصلي + attestation | 🟢 عالية | عالية جدًا |

**توصيتي:** تنفيذ الخطة كما هي، **مع إضافة قائمة IP اختيارية كطبقة ثانية** (§6.4). الجمع بين الموقع والـIP يرفع صعوبة التزوير كثيرًا بكلفة شبه معدومة.

---

## 3. طبقة صلاحيات الجمعية (المطلب الثاني)

### 3.1 قرار معماري: أين تُخزَّن الصلاحيات؟

هذه **أهم نقطة تقنية في الخطة**، وفيها خلل كامن في المخطط الحالي.

`CharityUser.permissions` حقل على **المستخدم**، بينما `CharityUserCharity` تسمح للمستخدم بالانتماء إلى **عدة جمعيات**. النتيجة: لو كان شخص مدير موارد بشرية في الجمعية (أ) وموظفًا عاديًا في الجمعية (ب)، فإن مصفوفة صلاحيات واحدة **لا تستطيع التعبير عن ذلك** — وسيحمل صلاحياته الإدارية معه إلى الجمعية (ب). هذا تسريب صلاحيات.

**القرار الموصى به:** نقل الصلاحيات إلى `CharityUserCharity.permissions` (صلاحيات لكل جمعية على حدة).

- منطقي: الميزة كلها عن "مدير الجمعية يمنح صلاحيات **داخل جمعيته**"
- الترحيل نظيف: الحقل الحالي **غير مستخدَم إطلاقًا**، فلا توجد بيانات فعلية تُنقل
- `getSession()` تُحمّل صلاحيات الجمعية النشطة (`session.charityId`) فقط
- يُترك `CharityUser.permissions` مهجورًا (أو يُحذف لاحقًا في تنظيف منفصل)

### 3.2 وحدة الصلاحيات الجديدة — `src/lib/charityPermissions.ts`

```ts
export function isCharitySystemAdmin(title?: string | null): boolean {
  return title === "SYSTEM_ADMIN";
}

export function hasCharityPermission(
  title: string | null | undefined,
  permissions: string[],
  required: string
): boolean {
  if (isCharitySystemAdmin(title)) return true;   // النظير المباشر لـ isAdmin()
  return permissions.includes(required);
}
```

`SYSTEM_ADMIN` هو **النظير الحرفي لـ`ADMIN`** في جانب الموظفين، والتحقق منه بالاسم هو الاستثناء الوحيد المسموح — تمامًا كما هو موثّق في صدر `lib/permissions.ts`. **كل ما عداه يمر عبر مصفوفة الصلاحيات، بلا أي قائمة ألقاب مثبّتة يدويًا.** (هذا التزام مباشر بنتيجة التدقيق الأمني الذي أُجري في 17 أغسطس.)

### 3.3 قائمة صلاحيات الجمعية المقترحة

```
── صلاحيات الإدارة ──
manage_charity_users     إدارة حسابات موظفي الجمعية
manage_attendance        إعداد مواقع العمل وأوقات الدوام
view_attendance_reports  عرض تقارير حضور جميع الموظفين

── صلاحيات الصفحات ──
view_services            الخدمات
view_governance          الحوكمة
view_design_requests     عرض طلبات التصاميم
create_design_requests   رفع طلب تصميم
view_hr                  صفحة الموارد البشرية
```

**تسجيل الحضور الشخصي ليس صلاحية** — كل من يملك حسابًا نشطًا يستطيع تسجيل حضوره هو. الصلاحية مطلوبة فقط لرؤية حضور *الآخرين*.

### 3.4 قاعدة منع تصعيد الصلاحيات (إلزامية)

عند منح الصلاحيات، يجب فرض ما يلي في الخادم:

1. **قاعدة المجموعة الجزئية:** لا يمنح المستخدم صلاحية لا يملكها هو (عدا `SYSTEM_ADMIN` الذي يملك الكل).
2. **لا ترقية إلى `SYSTEM_ADMIN`** إلا من `SYSTEM_ADMIN`.
3. **لا يعدّل المستخدم صلاحيات نفسه** إطلاقًا (يمنع سيناريو "أمنح نفسي كل شيء").
4. النطاق محصور بـ`charityId` المطابق للجلسة — يُفرض عبر `requireCharityMembership` الموجود في `lib/guards.ts`.

### 3.5 الحراسة على مستوى الصفحات

الفحص يجب أن يكون **في الخادم داخل كل صفحة RSC**، لا في القائمة الجانبية فقط. إخفاء عنصر من القائمة تجميل بحت — الرابط يبقى قابلًا للكتابة يدويًا. (هذا بالضبط الدرس المستفاد من ثغرة `custom-surveys` في التدقيق الأخير: شاشة محمية تنادي مسارًا مكشوفًا.)

يُضاف إلى `src/lib/guards.ts`:

```ts
export async function requireCharityPermission(charityId: string, permission: string)
```
مبنية فوق `requireCharityMembership` القائمة (التي تتحقق من العضوية عبر جدول الربط، لا عبر `session.charityId` وحده).

---

## 4. إدارة حسابات الموظفين (المطلب الأول)

### 4.1 مسار الإقلاع (Bootstrap)

مشكلة البيضة والدجاجة: من ينشئ أول `SYSTEM_ADMIN` في الجمعية؟

**الحل:** موظفو زاد عبر الشاشة القائمة `/main/charity-accounts` (التي تشترط `manage_charity_accounts`). يُنشئون حسابًا واحدًا بلقب `SYSTEM_ADMIN`، ثم يتولى هو إنشاء بقية الحسابات داخل جمعيته. **لا حاجة لتعديل هذه الشاشة.**

### 4.2 الإجراءات الجديدة — `src/app/actions/charityHr.ts`

| الإجراء | الحارس |
|---|---|
| `listCharityStaff(charityId)` | `manage_charity_users` |
| `createCharityStaff(charityId, {name, phone, title, permissions})` | `manage_charity_users` + قواعد §3.4 |
| `updateCharityStaffPermissions(...)` | `manage_charity_users` + قواعد §3.4 |
| `deactivateCharityStaff(...)` | `manage_charity_users` |

### 4.3 تعارض رقم الجوال — مسألة تحتاج قرارك

`CharityUser.phone` عليه قيد `@unique` **على مستوى النظام كله**، لا على مستوى الجمعية. فإذا حاول مدير الجمعية (أ) إنشاء حساب برقم مسجَّل مسبقًا في الجمعية (ب)، سيفشل الإنشاء.

الخيارات:

| الخيار | الوصف | التقييم |
|---|---|---|
| **(أ) رفض بسيط** | رسالة "هذا الرقم مسجّل مسبقًا في النظام" | ✅ **الموصى به** — بسيط وآمن |
| (ب) ربط تلقائي | إضافة الجمعية الجديدة للمستخدم القائم | ❌ خطير: يكشف لمدير الجمعية (أ) وجود الشخص في جمعية أخرى، ويمنحه وصولًا لحساب لم ينشئه |
| (ج) رفع القيد | جعل التفرد `[phone, charityId]` | ❌ يكسر تسجيل الدخول: `verifyCharityOTP` تبحث بالجوال وحده |

**التوصية: (أ).** الخيار (ج) خطر حقيقي لأنه يجعل رقمًا واحدًا يطابق عدة حسابات فينكسر مسار تسجيل الدخول بالكامل.

### 4.4 الحذف = تعطيل، وليس حذفًا

`CharityUserCharity` عليها `onDelete: Cascade`. وسجلات الحضور سترتبط بالمستخدم. **الحذف الفعلي سيمحو تاريخ الحضور كاملًا** — وهو سجل قد يكون له أثر نظامي/مالي.

**القرار: التعطيل فقط** (`isActive = false`). `getSession()` ترفض الحسابات غير النشطة أصلًا (`lib/auth.ts:47`)، فالتعطيل يمنع الدخول فورًا مع الحفاظ على السجل.

---

## 5. نظام التحضير الإلكتروني (المطلب الثالث)

### 5.1 نماذج قاعدة البيانات

```prisma
enum AttendanceStatus { PRESENT LATE EARLY_LEAVE ABSENT }

// موقع العمل — يحفظه صاحب الصلاحية في الجمعية
model CharityWorkSite {
  id           String   @id @default(uuid())
  charityId    String
  charity      Charity  @relation(fields: [charityId], references: [id], onDelete: Cascade)
  name         String                       // "المقر الرئيسي"
  latitude     Float
  longitude    Float
  radiusMeters Int      @default(150)       // نطاق السماح
  isActive     Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  attendance   AttendanceRecord[]
  @@index([charityId, isActive])
}

// أوقات الدوام لكل جمعية
model CharityWorkSchedule {
  id               String  @id @default(uuid())
  charityId        String  @unique
  charity          Charity @relation(fields: [charityId], references: [id], onDelete: Cascade)
  startTime        String  @default("08:00")   // بتوقيت الرياض
  endTime          String  @default("16:00")
  lateAfterMinutes Int     @default(15)
  workDays         Int[]   @default([0,1,2,3,4]) // الأحد..الخميس
  updatedAt        DateTime @updatedAt
}

model AttendanceRecord {
  id            String  @id @default(uuid())
  charityUserId String
  user          CharityUser @relation(fields: [charityUserId], references: [id], onDelete: Cascade)
  charityId     String
  charity       Charity @relation(fields: [charityId], references: [id], onDelete: Cascade)
  workSiteId    String?
  workSite      CharityWorkSite? @relation(fields: [workSiteId], references: [id], onDelete: SetNull)

  workDate      DateTime          // التاريخ المدني بتوقيت الرياض (منتصف الليل)

  checkInAt        DateTime?
  checkInLat       Float?
  checkInLng       Float?
  checkInAccuracy  Float?         // دقة القراءة بالمتر كما أبلغ عنها الجهاز
  checkInDistance  Float?         // المسافة المحسوبة في الخادم

  checkOutAt       DateTime?
  checkOutLat      Float?
  checkOutLng      Float?
  checkOutAccuracy Float?
  checkOutDistance Float?

  status        AttendanceStatus @default(PRESENT)

  // بيانات المراجعة والتدقيق
  ipAddress        String?
  userAgent        String?
  isSuspicious     Boolean @default(false)
  suspiciousReason String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([charityUserId, workDate])   // سجل واحد لكل موظف في اليوم
  @@index([charityId, workDate])
}
```

**ملاحظتان على المخطط:**
- `@@unique([charityUserId, workDate])` يمنع التحضير المكرر بنيويًا، لا بمنطق تطبيقي قابل لسباق التزامن (race condition).
- `workDate` يجب أن يُحسب **في الخادم** بتوقيت الرياض. الاعتماد على تاريخ الجهاز يسمح بتزوير اليوم بتغيير ساعة الهاتف.

### 5.2 وحدة الحساب الجغرافي — `src/lib/geo.ts`

```ts
export function haversineMeters(aLat, aLng, bLat, bLng): number
export function isWithinGeofence(distance, radiusMeters, accuracy): {...}
```

**قاعدة حاسمة:** المسافة تُحسب **في الخادم دائمًا**. العميل يرسل الإحداثيات الخام فقط، ولا يُقبل منه إطلاقًا حقل مثل `isWithinRange` — وإلا فالتزوير يصبح تعديل قيمة `true` في الطلب.

### 5.3 التعامل مع دقة القراءة (`accuracy`)

`getCurrentPosition` تُرجع `coords.accuracy` بالمتر. القراءة عبر شبكة Wi-Fi في المدن قد تصل دقتها إلى 50–2000 متر، وهي مشكلة عملية حقيقية:

- **دقة سيئة جدًا** (مثلًا > 200 متر) → **لا يُرفض التحضير فورًا**، بل تُطلب إعادة المحاولة مع رسالة واضحة ("تعذّر تحديد موقعك بدقة كافية، تأكد من تفعيل GPS")، لأن الرفض القاطع سيمنع موظفين صادقين داخل مبنى خرساني.
- تُسجَّل قيمة `accuracy` دائمًا في السجل للمراجعة.
- خيار الإعداد: `radiusMeters` يجب أن يكون سخيًا (100–200م) لاستيعاب خطأ القياس الطبيعي.

### 5.4 رصد الأنماط المشبوهة (تلقائي)

يُحسب في الخادم عند كل تحضير ويُخزَّن في `isSuspicious`:

1. **السفر المستحيل:** تحضيران من نفس الحساب في موقعين متباعدين خلال زمن يستحيل قطعه.
2. **دقة مثالية مريبة:** `accuracy` صغيرة جدًا وثابتة تمامًا عبر أيام (بصمة نموذجية للمواقع المزوّرة يدويًا).
3. **إحداثيات متطابقة تمامًا** بين أيام متعددة — الموقع الحقيقي يتذبذب دائمًا بضعة أمتار.

هذه **مؤشرات للمراجعة البشرية**، لا أحكامًا آلية. لا يُرفض التحضير بسببها بل يُعلَّم للمراجعة.

---

## 6. الواجهات

### 6.1 صفحة الموظف — تسجيل الحضور
`/portal/[name]/hr/attendance` — متاحة لكل حساب نشط:
- زر كبير واحد: **تسجيل حضور** / **تسجيل انصراف**
- طلب إذن الموقع مع شرح واضح لسبب الطلب
- عرض حالة اليوم والمسافة عن الموقع
- سجل حضور الشهر الشخصي

### 6.2 شاشة الإدارة — الموظفون
`/portal/[name]/hr` — تتطلب `manage_charity_users`:
- قائمة الموظفين، إضافة، تعطيل
- مُحرِّر صلاحيات بمجموعات (يعرض فقط ما يملكه المانح — §3.4)

### 6.3 شاشة الإدارة — الإعدادات والتقارير
`/portal/[name]/hr/settings` — تتطلب `manage_attendance`:
- **تحديد موقع العمل على خريطة** + ضبط النطاق
- أوقات الدوام وأيامه
`/portal/[name]/hr/reports` — تتطلب `view_attendance_reports`

### ⚠️ مسألة الخريطة تحتاج قرارك
سياسة CSP الحالية (`next.config.ts`) تحصر `img-src` و`connect-src` في `'self'` و`res.cloudinary.com` فقط. **أي مزود خرائط (Google Maps / Mapbox / OpenStreetMap) سيُحجب فورًا** ما لم تُضَف نطاقاته صراحةً.

الخيارات:
1. **بلا خريطة** (الأبسط): زر "استخدام موقعي الحالي" يملأ الإحداثيات + إدخال يدوي. **يفي بالمطلوب تمامًا** ولا يمس CSP.
2. **مع خريطة**: يتطلب توسيع CSP + اختيار مزود (وربما مفتاح API ومصاريف).

**توصيتي: الخيار 1 في المرحلة الأولى.** مدير الجمعية يقف في المقر ويضغط "استخدام موقعي الحالي" مرة واحدة — وهي في الواقع أدق طريقة لتحديد الموقع.

### 6.4 طبقة الـIP الاختيارية (موصى بها)
حقل اختياري `allowedIpRanges` على الجمعية. إن كان مضبوطًا، يُشترط أن يأتي التحضير من داخله (يُقرأ عبر `getClientIp()` **الموجودة مسبقًا** في `lib/auditLog.ts`). إن كان فارغًا فلا يُطبَّق شيء. طبقة أقوى من GPS بكلفة شبه معدومة.

---

## 7. الملفات

**جديدة:** `lib/charityPermissions.ts`, `lib/geo.ts`, `actions/charityHr.ts`, `actions/attendance.ts`, وصفحات `portal/[name]/hr/{page,attendance,settings,reports}` مع مكوّنات العميل.

**معدَّلة:**
- `prisma/schema.prisma` — النماذج الثلاثة + `CharityUserCharity.permissions` + العلاقات العكسية
- `lib/auth.ts` — تحميل صلاحيات الجمعية النشطة في `getSession()`
- `lib/guards.ts` — `requireCharityPermission`
- `portal/[name]/CharitySidebar.tsx` — تفعيل عنصر الموارد البشرية + إخفاء العناصر حسب الصلاحية
- `portal/[name]/layout.tsx` — تمرير الصلاحيات للقائمة الجانبية

**قاعدة البيانات:** `npx prisma db push` ثم `npx prisma generate` (لا يوجد مجلد migrations في المشروع). التعديل **إضافي غير هدّام** — الحقل الوحيد المنقول (`permissions`) فارغ وغير مستخدَم.

---

## 8. مراحل التنفيذ المقترحة

| المرحلة | المحتوى | قابلة للتسليم وحدها |
|---|---|---|
| **1** | طبقة صلاحيات الجمعية + حراسة الصفحات القائمة | ✅ قيمة أمنية فورية مستقلة |
| **2** | إدارة حسابات الموظفين | ✅ |
| **3** | نماذج الحضور + إعداد الموقع والدوام | ✅ |
| **4** | تسجيل الحضور والانصراف + الرصد | ✅ |
| **5** | التقارير والتصدير | ✅ |

بعد كل مرحلة: `npx tsc --noEmit` + `npm run build` + اختبار يدوي بحسابين (مدير نظام جمعية + موظف عادي).

---

## 9. التحقق

1. **تصعيد الصلاحيات:** محاولة منح صلاحية لا يملكها المانح → رفض. محاولة تعديل صلاحيات النفس → رفض. محاولة الترقية إلى `SYSTEM_ADMIN` من غير مدير نظام → رفض.
2. **العزل بين الجمعيات:** حساب في الجمعية (أ) يحاول إدارة موظفي (ب) عبر تعديل `charityId` في الطلب → رفض. حساب مرتبط بجمعيتين يتأكد أن صلاحياته في كلٍّ منهما منفصلة فعلًا.
3. **حراسة الصفحات:** موظف بلا `manage_charity_users` يكتب `/portal/<جمعيته>/hr` يدويًا → رفض (وليس مجرد إخفاء من القائمة).
4. **النطاق الجغرافي:** محاكاة إحداثيات داخل النطاق/على حدّه/خارجه عبر DevTools والتحقق من قرار الخادم.
5. **رفض ثقة العميل:** إرسال طلب تحضير يدويًا يحوي حقلًا مزيّفًا مثل `distance: 0` والتأكد أن الخادم يتجاهله ويحسب المسافة بنفسه.
6. **منع التكرار:** طلبا تحضير متزامنان لنفس الموظف واليوم → واحد ينجح فقط (قيد التفرد).
7. **التوقيت:** تحضير قرب منتصف الليل بتوقيت الرياض يُنسب لليوم المدني الصحيح، وتغيير ساعة الجهاز لا يؤثر.
8. **التعطيل:** تعطيل موظف يمنع دخوله فورًا مع بقاء سجل حضوره كاملًا.

---

## 10. قرارات مطلوبة منك قبل البدء

1. **هل تكفي طبيعة "الرادع + السجل"**، أم أن منع التزوير مطلب فعلي؟ (إن كان فعليًا → نناقش قائمة IP أو QR قبل كتابة أي كود) — §2
2. **نقل الصلاحيات إلى مستوى الجمعية** (`CharityUserCharity`) بدل مستوى المستخدم — موافق؟ §3.1
3. **تعارض رقم الجوال**: الرفض البسيط (توصيتي) أم غيره؟ §4.3
4. **الخريطة**: بلا خريطة في المرحلة الأولى (توصيتي) أم توسيع CSP لمزود خرائط؟ §6.3
5. **طبقة الـIP الاختيارية**: أضيفها؟ §6.4
6. **الانصراف**: هل تسجيل الانصراف مطلوب أصلًا، أم التحضير فقط؟ (يبسّط المرحلة 4 كثيرًا إن لم يكن مطلوبًا)

---

*خطة للمراجعة — لم يُكتب أي كود.*
