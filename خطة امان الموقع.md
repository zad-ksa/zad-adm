# تحليل سياسة الخصوصية ومعايير الأمن السيبراني السعودي لمنصة زاد

## ملخص تحليل الموقع

منصة **زاد التنموية** هي نظام إدارة جمعيات خيرية شامل مبني على Next.js 16 + PostgreSQL (Supabase) + Prisma ORM. يتضمن:

### أنواع المستخدمين
| النوع | آلية الدخول | الصلاحيات |
|-------|-----------|-----------|
| **موظفو زاد** (Employee) | OTP عبر Authentica أو كلمة مرور | لوحة تحكم `/main` - أدوار متعددة (ADMIN, EXECUTIVE_DIRECTOR, etc.) |
| **مستخدمو الجمعيات** (CharityUser) | OTP عبر Authentica | بوابة الجمعية `/portal/[name]` |
| **زوار الاستبيانات** | بدون تسجيل | صفحات الاستبيانات العامة |

### البيانات المُجمّعة والمُعالجة

| فئة البيانات | الحقول | مستوى الحساسية |
|-------------|--------|---------------|
| **بيانات الموظفين** | الاسم، الجوال، كلمة المرور (مُشفّرة bcrypt)، الصورة، الدور، الصلاحيات | 🔴 عالي |
| **بيانات الجمعيات** | الاسم، رقم الترخيص، تاريخ التأسيس، البريد، الجوال، أسماء المسؤولين وأرقامهم | 🔴 عالي |
| **البيانات المالية** | قيمة العقود، المبالغ المدفوعة، المنح، الأقساط، طلبات المنح، إيرادات سنوية | 🔴 عالي جداً |
| **حسابات الجهات المانحة** (DonorAccount) | اسم الجهة، اسم المستخدم، **كلمة المرور (نص عادي!)**, الموقع | 🔴🔴 حرج |
| **الاجتماعات** | العنوان، الملاحظات، المحتوى المنسّق، الحاضرون، المهام، ملخص AI | 🟠 متوسط-عالي |
| **المهام والمنجزات** | العنوان، المسؤول، إثبات الإنجاز (رابط ملف)، ملاحظات | 🟠 متوسط |
| **الاستبيانات** | اسم الجمعية، اسم المفوض، الإجابات، المرفقات | 🟠 متوسط |
| **ملفات مرفوعة** | صور، مستندات عبر Cloudinary | 🟠 متوسط |

---

## القسم الأول: سياسات الخصوصية المطلوبة

> [!IMPORTANT]
> يجب إنشاء صفحة `/privacy-policy` وصفحة `/terms` في الموقع وربطهما في Footer كل الصفحات. هذا مطلب إلزامي بموجب نظام حماية البيانات الشخصية السعودي (PDPL).

### 1. سياسة الخصوصية الكاملة (نص مقترح)

```
سياسة الخصوصية — منصة زاد التنموية
آخر تحديث: [التاريخ]

1. مقدمة
تلتزم شركة زاد للخدمات التنموية ("الشركة"، "نحن") بحماية خصوصية بياناتكم الشخصية 
وفقاً لنظام حماية البيانات الشخصية الصادر بالمرسوم الملكي رقم (م/19) وتاريخ 1443/2/9هـ 
ولائحته التنفيذية. توضح هذه السياسة كيفية جمع واستخدام وحماية بياناتكم عند استخدام منصتنا.

2. البيانات التي نجمعها

2.1 بيانات التسجيل والمصادقة:
- الاسم الكامل
- رقم الجوال (يُستخدم للتحقق عبر OTP)
- كلمة المرور (تُخزّن مُشفّرة)
- المسمى الوظيفي / الدور

2.2 بيانات الجمعيات الخيرية:
- اسم الجمعية ورقم الترخيص وتاريخ التأسيس
- بيانات التواصل (البريد الإلكتروني، أرقام الهاتف)
- أسماء وأرقام المسؤولين (رئيس مجلس الإدارة، المدير التنفيذي)
- البيانات المالية (قيم العقود، المنح، الأقساط)
- البرامج وأعداد المستفيدين

2.3 بيانات التشغيل:
- محاضر الاجتماعات والملاحظات
- المهام والمنجزات وإثباتاتها
- إجابات الاستبيانات
- الملفات والمرفقات المرفوعة
- سجلات الطلبات والاعتمادات

2.4 بيانات تقنية:
- عنوان IP ونوع المتصفح (عبر Vercel Analytics)
- بيانات الجلسة (JWT مشفّر في ملفات تعريف الارتباط)

3. أغراض المعالجة (الأساس القانوني)
نعالج بياناتكم للأغراض التالية:
- تقديم خدمات الاستشارات والتطوير للجمعيات الخيرية (تنفيذ العقد)
- التحقق من الهوية والمصادقة (المصلحة المشروعة)
- إعداد التقارير ومتابعة الأداء (تنفيذ العقد)
- تحسين المنصة وتجربة المستخدم (المصلحة المشروعة)
- الامتثال للمتطلبات النظامية (الالتزام القانوني)

4. مشاركة البيانات مع أطراف ثالثة
نشارك بياناتكم مع الجهات التالية فقط:
- Authentica (للتحقق من OTP عبر الرسائل النصية)
- Cloudinary (لتخزين الملفات والصور المرفوعة)
- Supabase (لاستضافة قاعدة البيانات)
- Vercel (لاستضافة المنصة والتحليلات)
- Anthropic Claude AI (لتنسيق محاضر الاجتماعات — بيانات مجهولة الهوية)

5. حقوق أصحاب البيانات (بموجب PDPL)
يحق لكم:
- الاطلاع على بياناتكم الشخصية المحفوظة لدينا
- طلب تصحيح البيانات غير الدقيقة
- طلب حذف البيانات (حق الإتلاف)
- طلب تقييد المعالجة
- الاعتراض على المعالجة
- نقل البيانات إلى جهة أخرى
للتواصل: [بريد مسؤول حماية البيانات]

6. مدة الاحتفاظ بالبيانات
- بيانات الحسابات: طوال فترة النشاط + 5 سنوات بعد إلغاء التفعيل
- البيانات المالية: 10 سنوات (متطلب نظامي)
- سجلات الاستبيانات: 3 سنوات
- سجلات الاجتماعات: 5 سنوات
- ملفات تعريف الارتباط: 30 يوماً (مدة الجلسة)

7. حماية البيانات
نطبق الإجراءات التالية لحماية بياناتكم:
- تشفير كلمات المرور باستخدام bcrypt
- تشفير جلسات المستخدم باستخدام JWT (HS256)
- ملفات تعريف ارتباط آمنة (httpOnly, secure, sameSite)
- نظام صلاحيات متعدد المستويات
- التحقق بخطوتين عبر OTP

8. ملفات تعريف الارتباط (Cookies)
نستخدم الأنواع التالية:
- session: ملف جلسة المصادقة (ضروري، 30 يوماً)
- dev_employee_override: للتطوير فقط (24 ساعة)
- Vercel Analytics: تحليلات الاستخدام

9. النقل الدولي للبيانات
قد تُنقل بياناتكم إلى خوادم خارج المملكة:
- Supabase (أستراليا - ap-southeast-2)
- Cloudinary (خوادم عالمية)
- Vercel (خوادم عالمية)
نضمن حماية مكافئة وفقاً لمتطلبات PDPL للنقل الدولي.

10. التعديلات على السياسة
نحتفظ بحق تعديل هذه السياسة. سيتم إشعاركم بأي تغييرات جوهرية.

11. التواصل
مسؤول حماية البيانات: [الاسم]
البريد الإلكتروني: [البريد]
الهاتف: [الرقم]
```

---

## القسم الثاني: تحليل الثغرات الأمنية ومعايير الكود السعودي

> [!CAUTION]
> تم اكتشاف عدة ثغرات أمنية حرجة يجب معالجتها فوراً قبل أي إطلاق إنتاجي.

### 🔴 ثغرات حرجة (يجب إصلاحها فوراً)

#### 1. كلمات مرور الجهات المانحة مخزّنة بنص عادي!
**الملف:** [charity.ts](file:///c:/Users/User/Desktop/zad_istbian/src/app/actions/charity.ts#L355-L365)
**المشكلة:** دالة `addDonorAccount` تخزّن كلمة المرور في حقل `password` بدون تشفير
**المخالفة:** مخالفة مباشرة لمعيار ECC-1-2018 (الضوابط الأساسية للأمن السيبراني) — البند 2-6-3
```diff
- password,
+ password: await hash(password, 12),
```

#### 2. ملف `.env` يحتوي على أسرار حساسة مكشوفة
**الملف:** [.env](file:///c:/Users/User/Desktop/zad_istbian/.env)
**المشكلة:** كلمة مرور قاعدة البيانات، مفاتيح Cloudinary، JWT Secret مباشرة في الملف
**الإجراء:** التأكد من أن `.env` مدرج في `.gitignore` وعدم رفعه لأي مستودع

#### 3. لا يوجد Rate Limiting
**المشكلة:** لا يوجد حد لمحاولات تسجيل الدخول أو إرسال OTP
**المخالفة:** ECC-1-2018 البند 2-6-2 (حماية من هجمات القوة العمياء)
**المطلوب:** إضافة rate limiting على routes المصادقة

#### 4. JWT Secret ضعيف وثابت
**الملف:** [.env](file:///c:/Users/User/Desktop/zad_istbian/.env#L12)
**المشكلة:** `JWT_SECRET` يحتوي على أجزاء من كلمة مرور قاعدة البيانات
**المطلوب:** استخدام مفتاح عشوائي قوي (256-bit minimum)

#### 5. مدة الجلسة طويلة جداً (30 يوم)
**الملف:** [auth.ts](file:///c:/Users/User/Desktop/zad_istbian/src/app/actions/auth.ts#L80)
**المشكلة:** `maxAge: 60 * 60 * 24 * 30` — 30 يوماً بدون تجديد
**المخالفة:** NDMO Cloud Cybersecurity Standards — الحد الأقصى الموصى به 24 ساعة مع تجديد تلقائي

### 🟠 ثغرات متوسطة

#### 6. لا توجد Security Headers
**المشكلة:** لا يوجد CSP, X-Frame-Options, X-Content-Type-Options, HSTS
**المطلوب:** إضافة middleware أو next.config.ts headers

#### 7. رفع الملفات بدون تحقق
**الملف:** [upload/route.ts](file:///c:/Users/User/Desktop/zad_istbian/src/app/api/upload/route.ts)
**المشكلة:** لا يوجد:
- تحقق من حجم الملف الأقصى
- تحقق من نوع الملف (MIME type validation)
- فحص فيروسات
- تحقق من هوية المستخدم (لا يتحقق من الجلسة!)

#### 8. لا يوجد Audit Log شامل
**المشكلة:** لا يتم تسجيل عمليات تسجيل الدخول، تغيير الصلاحيات، أو الوصول للبيانات الحساسة
**المخالفة:** ECC-1-2018 البند 2-13 (سجلات المراقبة)

#### 9. Developer Mode Bypass
**الملف:** [permissions.ts](file:///c:/Users/User/Desktop/zad_istbian/src/lib/permissions.ts#L84)
**المشكلة:** `developer_mode` يتجاوز كل فحوصات الصلاحيات — يجب تقييده ببيئة التطوير فقط

#### 10. Dev Phone Bypass في الإنتاج
**الملف:** [authCharity.ts](file:///c:/Users/User/Desktop/zad_istbian/src/app/actions/authCharity.ts#L21-L66)
**المشكلة:** رقم الجوال `0553973917` يتجاوز OTP ويُنشئ حساب تلقائياً — يجب إزالته من الإنتاج

---

## القسم الثالث: متطلبات الكود السعودي للأمن السيبراني

### الأطر التنظيمية المطبّقة

| الإطار | الجهة | الانطباق |
|--------|------|---------|
| **ECC-1:2018** — الضوابط الأساسية للأمن السيبراني | الهيئة الوطنية للأمن السيبراني (NCA) | ✅ إلزامي |
| **PDPL** — نظام حماية البيانات الشخصية | SDAIA | ✅ إلزامي |
| **CSCC** — ضوابط الأمن السيبراني للحوسبة السحابية | NCA | ✅ إلزامي (Supabase/Vercel) |
| **NDMO Standards** — معايير البيانات المفتوحة | مكتب إدارة البيانات الوطنية | 🟡 جزئي |

### خارطة الامتثال التفصيلية

#### ✅ ما هو محقق حالياً

| المتطلب | التطبيق الحالي |
|---------|---------------|
| تشفير كلمات المرور | bcrypt مع salt rounds = 10 |
| مصادقة متعددة العوامل | OTP عبر Authentica SMS |
| جلسات آمنة | JWT مع httpOnly + secure cookies |
| التحكم بالوصول (RBAC) | نظام أدوار وصلاحيات دقيق |
| فصل المستخدمين | فصل بين Employee و CharityUser |
| استخدام HTTPS | عبر Vercel (تلقائي) |

#### ❌ ما يجب تطبيقه

### 1. إدارة الهوية والوصول (ECC 2-2)

```
المطلوب:
□ سياسة تعقيد كلمات المرور (8+ أحرف، رموز، أرقام)
□ تغيير كلمة المرور الإجباري كل 90 يوماً
□ قفل الحساب بعد 5 محاولات فاشلة
□ Rate limiting على OTP (3 محاولات / 15 دقيقة)
□ تقصير مدة الجلسة (8 ساعات عمل + تجديد)
□ آلية تسجيل خروج تلقائي بعد فترة خمول
□ إزالة Dev Phone Bypass من الإنتاج
```

### 2. حماية البيانات (ECC 2-6 + PDPL)

```
المطلوب:
□ تشفير كلمات مرور DonorAccount (حرج!)
□ تشفير البيانات الحساسة at-rest (أرقام الهواتف، البيانات المالية)
□ إخفاء البيانات الحساسة في الواجهة (masking)
□ صفحة سياسة الخصوصية العامة
□ آلية موافقة المستخدم على جمع البيانات
□ آلية طلب حذف البيانات (حق الإتلاف)
□ سياسة احتفاظ بالبيانات مع آلية الحذف التلقائي
□ تقييم أثر حماية البيانات (DPIA)
```

### 3. أمن التطبيقات (ECC 2-9)

```
المطلوب:
□ إضافة Content Security Policy (CSP)
□ إضافة X-Frame-Options: DENY
□ إضافة X-Content-Type-Options: nosniff
□ إضافة Strict-Transport-Security (HSTS)
□ إضافة Referrer-Policy: strict-origin-when-cross-origin
□ إضافة Permissions-Policy
□ التحقق من المصادقة في upload route
□ التحقق من أنواع وأحجام الملفات المرفوعة
□ Input validation على كل Server Actions
□ حماية من CSRF (Next.js يوفر جزئياً عبر Server Actions)
```

### 4. سجلات المراقبة والتدقيق (ECC 2-13)

```
المطلوب:
□ تسجيل كل عمليات تسجيل الدخول/الخروج
□ تسجيل محاولات الدخول الفاشلة
□ تسجيل تغييرات الصلاحيات
□ تسجيل الوصول للبيانات المالية
□ تسجيل عمليات الحذف
□ تسجيل رفع/تحميل الملفات
□ تخزين السجلات لمدة لا تقل عن 12 شهراً
□ حماية السجلات من التعديل أو الحذف
```

### 5. أمن الحوسبة السحابية (CSCC)

```
المطلوب:
□ تقييم مخاطر مقدمي الخدمات السحابية (Supabase, Vercel, Cloudinary)
□ تشفير البيانات أثناء النقل (TLS 1.2+) ✅ محقق
□ تشفير البيانات في السكون (at-rest encryption)
□ النسخ الاحتياطي المنتظم لقاعدة البيانات
□ خطة استمرارية الأعمال والتعافي من الكوارث
□ اتفاقيات مستوى الخدمة مع مقدمي الخدمات
□ تقييم موقع تخزين البيانات (البيانات خارج المملكة حالياً!)
```

### 6. إدارة الحوادث (ECC 2-14)

```
المطلوب:
□ خطة استجابة للحوادث الأمنية
□ آلية إبلاغ NCA خلال 72 ساعة من اكتشاف الاختراق
□ آلية إبلاغ أصحاب البيانات المتأثرين
□ فريق استجابة محدد مع أدوار واضحة
```

---

## القسم الرابع: خطة التنفيذ المقترحة (بالأولوية)

### المرحلة 1: حرج — فوري (أسبوع واحد)
1. تشفير كلمات مرور `DonorAccount` باستخدام bcrypt
2. إزالة Dev Phone Bypass من كود الإنتاج
3. إضافة authentication check في `/api/upload`
4. إنشاء صفحة سياسة الخصوصية `/privacy-policy`
5. تقوية JWT Secret

### المرحلة 2: عالي — قريب (2-3 أسابيع)
1. إضافة Rate Limiting (على auth routes + OTP)
2. إضافة Security Headers (CSP, HSTS, X-Frame-Options)
3. تقصير مدة الجلسة + آلية التجديد
4. إضافة File validation في Upload route
5. إنشاء جدول `AuditLog` وتسجيل العمليات الحساسة

### المرحلة 3: متوسط — منتصف المدة (1-2 شهر)
1. تطبيق سياسة تعقيد كلمات المرور
2. آلية قفل الحساب بعد محاولات فاشلة
3. تقييد `developer_mode` ببيئة التطوير
4. النسخ الاحتياطي التلقائي لقاعدة البيانات
5. آلية حذف البيانات (حق الإتلاف - PDPL)

### المرحلة 4: تحسين — بعيد المدة (2-3 أشهر)
1. تقييم نقل البيانات إلى منطقة سعودية/خليجية
2. خطة استمرارية الأعمال
3. خطة الاستجابة للحوادث
4. تقييم أثر حماية البيانات (DPIA)
5. اختبار اختراق شامل

---

## مراجعة المستخدم المطلوبة

> [!IMPORTANT]
> ### قرارات تحتاج موافقتك:
> 1. **هل تريد البدء بالمرحلة 1 (الإصلاحات الحرجة) فوراً؟**
> 2. **هل تريد إنشاء صفحة سياسة الخصوصية كصفحة ويب كاملة في الموقع؟**
> 3. **هل المنصة تخدم جهات حكومية؟** (يؤثر على مستوى الامتثال المطلوب)
> 4. **هل هناك ميزانية لنقل البيانات إلى خوادم في المملكة؟** (Supabase حالياً في أستراليا)
> 5. **هل تريد تعيين مسؤول حماية بيانات (DPO)؟** (مطلوب بموجب PDPL للجهات التي تعالج بيانات حساسة)

> [!WARNING]
> ### تحذيرات عاجلة:
> - كلمات مرور الجهات المانحة **مكشوفة بنص عادي** في قاعدة البيانات — هذا يُعدّ اختراقاً أمنياً بحد ذاته
> - ملف `.env` يحتوي على credentials حقيقية — تأكد أنه **غير مرفوع** لأي مستودع Git عام
> - Dev Phone Bypass يسمح بدخول بدون OTP — يجب إزالته قبل الإطلاق
> - البيانات تُخزّن في أستراليا (Supabase ap-southeast-2) — قد يُخالف متطلبات توطين البيانات
